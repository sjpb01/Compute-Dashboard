# Binary intermediates — regenerated by the pipeline, not worth versioning.
out/*.parquet

# The dashboard (docs/index.html) and Excel-friendly CSVs (out/*.csv) ARE committed.

__pycache__/
*.pyc
.venv/
